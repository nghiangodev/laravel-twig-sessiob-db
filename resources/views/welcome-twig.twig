<!DOCTYPE html>
<html lang="{{ app.getLocale }}">
<head>
    <!-- other regular head stuff -->
</head>
<body class="antialiased">
<h1>Twig Demo</h1>
<div class="relative flex items-top justify-center min-h-screen bg-gray-100 dark:bg-gray-900 sm:items-center sm:pt-0">
    <p>test loop</p>
    {% for i in 0..10 %}
      <ul>
          <li>{{ i }}</li>
      </ul>
    {% endfor %}

        <br>

    <p>get data user</p>
    {% for user in users %}
        Username: {{ user.name }}
        email: {{ user.email }}
    {% endfor %}

    <p>get data with key</p>

    <ul>
        {% for key, user in users %}
            <li>{{ key }}: {{ user.name|e }}</li>
        {% endfor %}
    </ul>
</div>
</body>
</html>
